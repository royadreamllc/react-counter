import React from "react";
var Shape =(props) => {
  console.log();
  return (
     <div className={porps.shape}></div>

     const Shape= (props)=>{
       var shape= props.shape;
       var selectShape=props.selectShape
     }
      const selectedShape =props.selectShape
   )
}

export default Shape;
